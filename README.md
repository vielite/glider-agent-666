# GLider-Agent-666
CLI native Retrieval-Augmented Generation assistant for writing glider queries using `gemini-2.5-flash` model.

## Installation
Download repo
`git clone [this repo]`

Export gemini api key
`export GOOGLE_API_KEY=[your_api_key]`
